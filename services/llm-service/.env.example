# LLM Service Configuration

SERVICE_NAME=llm-service
PORT=8000

# Models Directory (mounted volume in Docker)
MODELS_DIR=/app/models

# Performance Settings
LLM_THREADS=8
LLM_GPU_LAYERS=35  # 0 for CPU-only, 35+ for GPU

# Logging
LOG_LEVEL=INFO
